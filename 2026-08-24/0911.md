# 台股市場廣度簡報

**產出時間**：2026-08-24 09:11（台北時間）
**資料交易日**：2026-08-24（盤中，交易時段剛開盤約 10 分鐘）

## 一、本次無法產出數據 — Taiwan-Stock MCP connector 完全無回應

本次嘗試查詢 Taiwan-Stock MCP connector（`get_realtime_market_breadth`、`get_realtime_top_movers`、
`get_realtime_volume_surge`、`get_market_overview`、`get_volume_surge_stocks` 等工具）時，**該
connector 在本次 session 中未曝露任何可呼叫的工具**（`ToolSearch` 以多組關鍵字及確切工具名稱查詢均回傳
「no matching deferred tools found」，且未出現「still connecting」的等待提示），已確認並非單純連線
逾時：

- 已重試 `ToolSearch` 逾 5 次，涵蓋關鍵字搜尋與確切工具名稱（`select:` 語法）查詢，包含間隔等待（累計逾
  20 秒）後再重試，結果一致為找不到任何 Taiwan-Stock 相關工具。
- `ListConnectors` 顯示 connector 名稱為 `Taiwan-Stock`、`enabledInChat: true`（本 session 已啟用），
  但 `installState` 回報為 `unknown`（無法確認實際連線狀態）。
- `ListMcpResourcesTool` 對 `Taiwan-Stock` 伺服器呼叫回傳「No resources found」而非「伺服器不存在」的
  錯誤，顯示 MCP 協定層級的連線本身可能有建立，但該伺服器完全未提供任何工具（`realtime_price`、
  `daily_price`、`basic_securities_information` 等資料表皆無法查詢）。

**與過去已知問題的差異**：本存放庫過去記錄的是 `realtime_price` 查詢層自 2026-08-06 起的
**部分**覆蓋率不足或 SQL 參數綁定錯誤（工具仍可呼叫，但回傳資料不完整或報錯）。本次則是
**connector 層級完全無工具可用**，屬於更嚴重的失效模式，上一份簡報（2026-08-21 13:15）仍記錄
所有相關工具正常回應、`realtime_price` 覆蓋率達 1,866 檔，故此次異常應是 2026-08-21 之後、
2026-08-24 開盤前新發生的狀況（本存放庫無 2026-08-22、23 的簡報，因台股當日休市）。

## 二、無法產出之項目

- 上漲／下跌／平盤家數、漲跌家數比（市場廣度）
- 平均漲跌幅、市場氣氛
- 漲幅前五名／跌幅前五名個股
- 成交量異常放大個股

以上項目均需仰賴 `realtime_price`（含 `daily_price` 比對）與 `basic_securities_information`
資料表，因 connector 本次完全無工具可用，故本次簡報**無任何替代或權宜數據**可提供，亦未使用任何
舊資料冒充本次快照。

## 三、建議

1. 檢查 Taiwan-Stock MCP connector 於 claude.ai 端的安裝／授權狀態是否正常（本次 `installState`
   回報為 `unknown`，建議人工確認該 connector 是否遭停用、憑證過期或後端服務下線）。
2. 若下一次排程執行時 connector 恢復曝露工具，請在簡報備註中註明「connector 自 2026-08-24 09:11
   起短暫完全無工具可用，於 <恢復時間> 恢復」，以利追蹤本次事件的起訖時間與其後 `realtime_price`
   覆蓋率是否受影響。
3. 若 connector 持續無回應，建議下次執行時仍嘗試查詢並如實記錄，勿略過本次排程或略過簡報產出。
