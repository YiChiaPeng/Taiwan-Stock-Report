# 台股市場廣度簡報

**產出時間**：2026-08-24 10:07（台北時間）
**資料交易日**：2026-08-24（盤中，開盤約 1 小時 7 分）

## 一、本次仍無法產出數據 — Taiwan-Stock MCP connector 持續無回應

本次嘗試查詢 Taiwan-Stock MCP connector（`get_realtime_market_breadth`、`get_realtime_top_movers`、
`get_realtime_volume_surge`、`get_market_overview`、`get_volume_surge_stocks` 等工具，以及直接查詢
`realtime_price`、`daily_price`、`basic_securities_information` 資料表所需的工具）時，**該 connector
在本次 session 中仍未曝露任何可呼叫的工具**，狀況與上一份簡報（2026-08-24 09:11）記錄的完全一致：

- `ToolSearch` 以多組關鍵字（含中英文、資料表名稱、確切工具名稱之 `select:` 語法）查詢逾 5 次，並穿插
  多次等待（累計逾 25 秒）後重試，結果一致為「no matching deferred tools found」，且過程中不再出現
  「still connecting」等待提示，顯示並非單純連線逾時。
- `ListConnectors` 顯示 connector 名稱為 `Taiwan-Stock`、`enabledInChat: true`（本 session 已啟用），
  但 `installState` 仍回報為 `unknown`（與 09:11 那次相同，無法確認實際連線狀態）。
- `ListMcpResourcesTool` 對 `Taiwan-Stock` 伺服器呼叫回傳「No resources found」而非伺服器不存在的錯誤，
  顯示 MCP 協定層級的連線可能已建立，但該伺服器仍完全未提供任何工具。

**與 09:11 簡報的關聯**：本次故障徵狀與 09:11 記錄的「connector 層級完全無工具可用」完全相同，顯示此次
連接器完全無回應的事件持續中，至少已橫跨 09:11–10:07（約 56 分鐘）未恢復。

## 二、無法產出之項目

- 上漲／下跌／平盤家數、漲跌家數比（市場廣度）
- 平均漲跌幅、市場氣氛
- 漲幅前五名／跌幅前五名個股
- 成交量異常放大個股

以上項目均需仰賴 `realtime_price`（含 `daily_price` 比對）與 `basic_securities_information` 資料表，
因 connector 本次仍完全無工具可用，故本次簡報**無任何替代或權宜數據**可提供，亦未使用任何舊資料冒充
本次快照。

## 三、建議

1. 建議人工儘速確認 Taiwan-Stock MCP connector 於 claude.ai 端的安裝／授權狀態（`installState` 持續回報
   `unknown`，且已橫跨兩次排程執行仍未恢復，故障時間已知至少約 1 小時）。
2. 若下一次排程執行時 connector 恢復曝露工具，請在簡報備註中註明「connector 自 2026-08-24 09:11 起完全
   無工具可用，於 <恢復時間> 恢復（故障持續約 <時長>）」，以利追蹤本次事件的完整起訖時間及其後
   `realtime_price` 覆蓋率是否受影響。
3. 若 connector 持續無回應，建議下次執行時仍嘗試查詢並如實記錄，勿略過本次排程或略過簡報產出。
