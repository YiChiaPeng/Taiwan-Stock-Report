# 台股市場廣度簡報

**產出時間**：2026-08-31 13:08（台北時間）
**資料交易日**：2026-08-31（台股開盤日，盤中，開盤約 4 小時 8 分鐘，距收盤約 22 分鐘）

## 一、本次無法產出數據 — Taiwan-Stock MCP connector 連線逾時（中斷持續中）

本次嘗試查詢 Taiwan-Stock MCP connector（`get_realtime_market_breadth`、`get_realtime_industry_movers`、
`get_realtime_volume_surge`、`get_market_overview`、`get_volume_surge_stocks` 等工具，以及直接查詢
`realtime_price`、`daily_price` 資料表所需的工具）時，**該 connector 在本次 session 中連線逾時，未曝露
任何可呼叫的工具**：

- `ToolSearch` 以確切工具名稱（單一與批次查詢皆嘗試）重複查詢，結果一致回報：「These configured MCP
  servers failed to connect: Taiwan-Stock (CONNECT_TIMEOUT): "MCP server Taiwan-Stock connection timed out
  after 30000ms"」。
- `ListConnectors` 顯示 connector 名稱為 `Taiwan-Stock`、`enabledInChat: true`（本 session 已啟用），但
  `installState` 仍回報為 `unknown`，與前四次記錄狀態相同，無法確認實際連線狀態。

**與前次事件的關聯**：此為 2026-08-31 09:09 首次記錄之 `CONNECT_TIMEOUT` 中斷事件的**第五次連續失敗**。
中斷已持續約 239 分鐘（3 小時 59 分，09:09 → 13:08），橫跨五次排程執行（09:09、10:07、11:07、12:09、
13:08），症狀不變（connector 完全無工具可用，非個別資料表之部分性降級）。本次交易日（2026-08-31）自開盤
09:00 起至今尚未有任何一次排程成功取得即時資料。

## 二、無法產出之項目

- 上漲／下跌／平盤家數、漲跌家數比（市場廣度）
- 平均漲跌幅、市場氣氛
- 領漲／領跌族群與代表個股（`get_realtime_industry_movers`）
- 依整點時段之成交量異常放大個股

以上項目均需仰賴 Taiwan-Stock MCP connector 提供的工具，因 connector 本次連線逾時、完全無工具可用，
故本次簡報**無任何替代或權宜數據**可提供，亦未使用任何舊資料冒充本次快照。

## 三、建議

1. 建議人工確認 Taiwan-Stock MCP connector 於 claude.ai 端的安裝／授權狀態（`installState` 已連續五次
   回報 `unknown`，連線逾時 30 秒，中斷持續逾 239 分鐘／約 4 小時，橫跨整個 2026-08-31 交易日至今）。
2. 若下一次排程執行時 connector 恢復曝露工具，請在簡報備註中註明「connector 自 2026-08-31 09:09 起
   連線逾時，於 <恢復時間> 恢復（故障持續約 <時長>）」，以利追蹤本次事件的完整起訖時間。
3. 若 connector 於今日收盤（13:30）前仍未恢復，2026-08-31 全日盤中將無任何市場廣度數據留存，建議收盤後
   改以 `daily_price` 資料表（待 connector 恢復後）回溯補充當日收盤總結，並在簡報中註明為事後回補資料。
4. 若 connector 持續無回應，建議下次執行時仍嘗試查詢並如實記錄，勿略過本次排程或略過簡報產出。
