# 台股市場廣度簡報

**產出時間**：2026-08-31 09:09（台北時間）
**資料交易日**：2026-08-31（台股開盤日，盤中，開盤約 9 分鐘）

## 一、本次無法產出數據 — Taiwan-Stock MCP connector 連線逾時

本次嘗試查詢 Taiwan-Stock MCP connector（`get_realtime_market_breadth`、`get_realtime_industry_movers`、
`get_realtime_volume_surge`、`get_market_overview`、`get_volume_surge_stocks` 等工具，以及直接查詢
`realtime_price`、`daily_price` 資料表所需的工具）時，**該 connector 在本次 session 中連線逾時，未曝露
任何可呼叫的工具**：

- `ToolSearch` 以確切工具名稱（如 `get_realtime_market_breadth`）重複查詢 6 次以上，橫跨約數分鐘，結果
  一致回報：「These configured MCP servers failed to connect: Taiwan-Stock (CONNECT_TIMEOUT): "MCP
  server Taiwan-Stock connection timed out after 30000ms"」。
- `ListConnectors` 顯示 connector 名稱為 `Taiwan-Stock`、`enabledInChat: true`（本 session 已啟用），但
  `installState` 回報為 `unknown`，無法確認實際連線狀態。

**與先前事件的關聯**：此次故障徵狀（`CONNECT_TIMEOUT`，連線逾時）與 2026-08-24 09:11 至 2026-08-27
09:11 記錄的「connector 完全無工具可用（zero tools exposed）」故障**症狀不同**，但同屬 connector 層級
的完全性中斷（並非個別資料表如 `realtime_price` 的部分性降級）。上次成功產出的簡報為 2026-08-28
13:10，之後歷經週末（08-29、08-30 非交易日），本次為 2026-08-31 開盤後首次排程查詢，即出現此連線逾時
故障，故本次視為一次新發生的獨立中斷事件起點（2026-08-31 09:09 首次記錄）。

## 二、無法產出之項目

- 上漲／下跌／平盤家數、漲跌家數比（市場廣度）
- 平均漲跌幅、市場氣氛
- 領漲／領跌族群與代表個股（`get_realtime_industry_movers`）
- 依整點時段之成交量異常放大個股

以上項目均需仰賴 Taiwan-Stock MCP connector 提供的工具，因 connector 本次連線逾時、完全無工具可用，
故本次簡報**無任何替代或權宜數據**可提供，亦未使用任何舊資料冒充本次快照。

## 三、建議

1. 建議人工確認 Taiwan-Stock MCP connector 於 claude.ai 端的安裝／授權狀態（`installState` 回報
   `unknown`，連線逾時 30 秒）。此為 2026-08-31 09:09 首次記錄的獨立中斷事件，若下次排程仍未恢復，
   請在後續簡報中比照 2026-08-24 事件的記法持續追蹤中斷時長與連續失敗次數。
2. 若下一次排程執行時 connector 恢復曝露工具，請在簡報備註中註明「connector 自 2026-08-31 09:09 起
   連線逾時，於 <恢復時間> 恢復（故障持續約 <時長>）」，以利追蹤本次事件的完整起訖時間。
3. 若 connector 持續無回應，建議下次執行時仍嘗試查詢並如實記錄，勿略過本次排程或略過簡報產出。
