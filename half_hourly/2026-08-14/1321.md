# 台股市場廣度簡報

**產出時間：** 2026-08-14 13:21（台北時間）
**資料交易日：** 2026-08-14（台股開盤日，盤中，距收盤約 9 分鐘）

## 執行結果：本次仍無法產出即時數據

> ⚠️ **`realtime_price` 查詢層持續故障中（後端 SQL 參數綁定錯誤，與 2026-08-06～2026-08-14 12:16 歷次相同）：** 本次呼叫 Taiwan-Stock MCP connector 中所有以 `realtime_price` 資料表為基礎的工具——`get_realtime_market_breadth`、`get_realtime_top_movers`（漲/跌方向皆同）、`get_realtime_volume_surge`——**全數再次回傳相同的 PostgreSQL 語法錯誤**：
>
> ```
> psycopg2.errors.SyntaxError: syntax error at or near ":"
> LINE 5: WHERE to_timestamp(timestamp)::date = :as_of_date::date
> ```
>
> 錯誤成因與先前歷次完全一致：伺服器端查詢同時混用 SQLAlchemy 風格的 `:as_of_date`（未被驅動正確處理）與 psycopg2 風格的 `%(limit)s`／`%(multiplier)s`／`%(min_volume_k)s`／`%(exchange)s` 等參數綁定語法。此為連接器後端程式碼問題，非資料缺漏，屬系統性故障，**自 2026-08-06 09:06 起持續未修復，已逾 196 小時（超過 8 天）、累計至少 32 次排程失敗（含本次）、影響 7 個交易日（08-06、08-07、08-10、08-11、08-12、08-13、08-14 已 5 次：09:14、10:09、11:13、12:16、13:21）**。

**次要限制：**
1. `daily_price` 資料表目前僅有截至 2026-08-13 收盤（上一交易日）的資料可用（`get_market_overview("2026-08-13")` 正常回傳），今日（2026-08-14）盤中尚無收盤資料入庫（`get_market_overview("2026-08-14")` 回傳 `no_data`），故無法以 `daily_price` 回退計算今日即時漲跌幅。今日約 13:30 收盤，資料入庫後才可能改用日資料工具。
2. `get_volume_surge_stocks` 依先前歷次測試同樣受相同語法錯誤影響，本次未重複測試，仍無法用於今日或上一交易日的成交量異常放大清單。

## 本次未能提供之項目

1. 上漲、下跌、平盤家數 — 無法計算（即時）
2. 漲跌家數比（市場廣度） — 無法計算（即時）
3. 漲幅前五名與跌幅前五名個股（即時） — 無法計算
4. 成交量異常放大個股（即時或當日） — 無法計算

## 權宜替代資料（僅供參考，非本次交易日即時數據）

**上一交易日（2026-08-13）收盤市場概況：**

| 指標 | 數值 |
|---|---|
| 上漲 | 788 家 |
| 下跌 | 981 家 |
| 平盤 | 176 家 |
| 總計 | 1,954 檔 |
| 漲跌家數比 | 0.80（788:981） |
| 平均漲跌幅 | +0.11% |
| 市場氣氛 | 中性（neutral） |

成交量異常放大清單本次無法提供（`get_volume_surge_stocks` 仍受相同參數綁定錯誤影響，本次未重複呼叫測試）。

## 建議

1. `realtime_price` 相關查詢工具（`get_realtime_market_breadth`、`get_realtime_top_movers`、`get_realtime_volume_surge`、`get_realtime_quote`、`get_realtime_bid_ask_imbalance`、`get_realtime_breadth_by_industry`）自 2026-08-06 09:06 起持續故障至今（今日 13:21），**已累計至少 32 次排程、超過 196 小時（逾 8 天）未修復，影響 7 個交易日**，此問題已於歷次簡報中重複回報，強烈建議儘速通知連接器維護者處理，統一改用 psycopg2 的 `%(name)s` 風格，或改用 SQLAlchemy 的 `text()` + `:name` 風格並確保驅動一致。
2. `get_volume_surge_stocks` 同樣存在相關缺陷，建議一併通知維護者排查。
3. 今日台股約 13:30 收盤，`daily_price` 入庫後，下一次排程可望改用 `get_market_overview("2026-08-14")` 等日資料工具，產出今日「收盤版」市場廣度簡報作為權宜替代。

本次無法提供即時漲跌家數統計、即時排行榜或即時成交量異常清單；上表僅為權宜替代之上一交易日資料，不代表今日（08-14）即時市場狀況。
