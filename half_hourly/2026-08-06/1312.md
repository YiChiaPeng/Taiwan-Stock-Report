# 台股市場廣度簡報

**產出時間：** 2026-08-06 13:12（台北時間）
**資料交易日：** 2026-08-06（台股開盤日，盤中）

## 執行結果：本次仍無法產出

> ⚠️ **`realtime_price` 查詢層持續故障中（後端 SQL 參數綁定錯誤，與 09:06、10:07、11:06、12:07 四次相同，已連續故障超過 4 小時）：** 本次呼叫 Taiwan-Stock MCP connector 中所有以 `realtime_price` 資料表為基礎的工具——`get_realtime_market_breadth`、`get_realtime_top_movers`（漲/跌方向皆同）、`get_realtime_volume_surge`、`get_realtime_quote`——**全數再次回傳相同的 PostgreSQL 語法錯誤**：
>
> ```
> psycopg2.errors.SyntaxError: syntax error at or near ":"
> LINE 5: WHERE to_timestamp(timestamp)::date = :as_of_date::date
> ```
>
> 錯誤成因與前四次一致：伺服器端查詢同時混用 SQLAlchemy 風格的 `:as_of_date`（未被驅動正確處理）與 psycopg2 風格的 `%(limit)s`／`%(multiplier)s`／`%(min_volume_k)s`／`%(tickers)s`／`%(exchange)s` 等參數綁定語法。此為連接器後端程式碼問題，非資料缺漏，且影響所有 `realtime_price` 相關工具（含個股查詢 `get_realtime_quote`），屬系統性故障，**自今日 09:06 起持續未修復（09:06 → 10:07 → 11:06 → 12:07 → 13:12，已逾 4 小時、連續 5 次排程失敗）**。

**次要限制：** 今日（2026-08-06）盤中，`daily_price` 尚無今日收盤資料（`get_market_overview` 回傳 `error_code: no_data`），需待約 13:30 收盤後入庫，故也無法以 `daily_price` 回退計算今日即時漲跌幅作為替代。

## 本次未能提供之項目

1. 上漲、下跌、平盤家數 — 無法計算
2. 漲跌家數比（市場廣度） — 無法計算
3. 漲幅前五名與跌幅前五名個股 — 無法計算
4. 成交量異常放大個股 — 無法計算

## 建議

1. `realtime_price` 相關查詢工具（`get_realtime_market_breadth`、`get_realtime_top_movers`、`get_realtime_volume_surge`、`get_realtime_quote`）自今日 09:06 該次排程起即持續故障至今（13:12），**已連續 5 次排程、超過 4 小時未修復**，建議儘速通知連接器維護者處理，統一改用 psycopg2 的 `%(name)s` 風格，或改用 SQLAlchemy 的 `text()` + `:name` 風格並確保驅動一致。
2. 若需盡快取得市場概況，可待今日約 13:30 收盤後、`daily_price` 入庫後，改用 `get_market_overview` 等日資料工具產出「收盤版」簡報作為權宜替代。

本次無統計數據、無排行榜、無成交量異常清單可供回報。
